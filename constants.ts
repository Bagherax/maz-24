import type { View } from './types';

export const VIEWS: { [key: string]: View } = {
  MARKETPLACE: 'marketplace',
  SEARCH: 'search',
  CREATE_AD: 'create_ad',
  CHAT: 'chat',
  PROFILE: 'profile',
};
